// API Configuration
// Using relative paths so Vite proxy works (all requests go through localhost:5173)

export const API_BASE_URL = '';  // Empty = relative, uses Vite proxy
export const WS_URL = 'ws://localhost:8000/ws';
export const AUTH_URL = '';
